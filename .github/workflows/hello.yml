name: Trigger Demo

on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

jobs:
  test-trigger:
    runs-on: ubuntu-latest

    steps:
      - name: Show trigger
        run: echo "GitHub Actions was triggered!"